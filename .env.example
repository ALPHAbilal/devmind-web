# Copy to .env.local (which is git-ignored) and fill in real values.
# Get the anon key from: Supabase dashboard → Project Settings → API.
NEXT_PUBLIC_SUPABASE_URL=https://rtakdeilcqdgczgjhtya.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ── Server-only (NEVER prefix with NEXT_PUBLIC_) ─────────────────────────
# The browser must NOT see these. They power the Vercel → Fly HMAC proxy
# (see spec/ROUTES_AND_CHANNELS.md §1). Used by app/api/missions/*/route.ts
# via lib/flyClient.ts.
FLY_BACKEND_URL=https://devmind-backend.fly.dev
FLY_BACKEND_INTERNAL_TOKEN=
